import { createTheme } from "@material-ui/core";

export const theme = createTheme({
    palette : {
        primary : {
            main : "#111",
        },
    },
    myButton : {
        backgroundColor : "red",
        color : "white",
        border : "1px solid black"
    },
})